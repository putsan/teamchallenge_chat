import { Button } from "@mui/material";
// eslint-disable-next-line no-unused-vars
function StyledButton({ text, style, size, onClick }) {
  return <Button variant="contained">{text}</Button>;
}
export default StyledButton;
